#include <iostream>
#include <map> 
using namespace std;


int main(){
    
    
map< int,map<string,double> > map_data;

map<string, double> data;

data.emplace("window", 3.14);

data.emplace("windoww", 3.16);

map_data.emplace(0, data);

map<string, double>::iterator itr1;
map<int, map<string, double> >::iterator itr2;
for(itr2 = map_data.begin(); itr2 != map_data.end(); itr2++)
{
for(itr1 = data.begin(); itr1 != data.end(); itr1++)
{
cout << "\n*^*Window: " << itr2->first << " \ " << itr1->first << " \ " << itr1->second;
}
}
    
    
    return 0;

}